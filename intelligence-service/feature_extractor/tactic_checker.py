from sudoku import Sudoku
import numpy as np

def check_tactics(board_array):
    """Uses the py-sudoku library to extract high-level difficulty metrics for the model."""
    #Convert 0s to none (required by the library)
    board_list = board_array.tolist()
    for r in range(9):
        if board
def _find_x_wing(board):
    #logic will go here
    return 0 #temp

